public enum GroundTileTypeEnum
{
    Hedge,
    Grass,
    Stone,
    Water,
    PressurePlate,
    Door,
    RuneSource,    // stays on ground layer — it IS the ground
    RuneReceiver,  // stays on ground layer — it IS the ground
    // RuneChannelHorizontal ← REMOVED
    // RuneChannelVertical   ← REMOVED
}