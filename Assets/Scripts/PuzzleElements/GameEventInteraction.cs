using UnityEngine;

public class GameEventInteraction : MonoBehaviour
{

    // move the door pressure plate logic here
    // add light receiver logic here

    // when conditions are met, have the open to show or hide object
    // when either way, it should register that the item is on/off the grid

    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
